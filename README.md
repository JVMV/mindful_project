# The mindful project is a simple todo app.
It is built on React and uses Redux state management for tracking form input and todoList items. The mindful project uses axios for requests to multiple APIs.